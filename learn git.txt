craete git repo in desktop
$git init

add files
$git add .

save files
$git commit -m "message"

upload files for any brnch
$git push -u origin name_branch(master)

take copy of files for first times after that git pull
$git clone (link) https://github.com/Fady-Moawad/learnGit.git

link GitHub
$git remote add origin (link) https://github.com/YOUR_NAME/hello-project.git

// base branch main or master , master is defualt branch

see all branches 
$git branch

create new branch
$git branch name_branch

swap branch
$git checkout name_branch_want_to_go

merge لازم تبقى واقف على الفرع اللي هيستقبل التغييرات و تعمل دمج مع الفرع اللي عاوز تنقل منه
$git merge name_branch
master الي test عاوز تنقل شغل من 

الخطوات الصح:
git checkout master     ← اقف على اللي هيستقبل
git merge test          ← دمج test جوا master
git push origin master  ← ارفع التغيير على GitHub
push لازم تعمل merge خلي بالك بعد ما تعمل 